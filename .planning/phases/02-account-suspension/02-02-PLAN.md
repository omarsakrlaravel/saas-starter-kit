---
phase: 02-account-suspension
plan: 02
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - app/Http/Middleware/AccountStatusMiddleware.php
  - app/Http/Controllers/AccountRestrictedController.php
  - bootstrap/app.php
  - routes/web.php
  - routes/api.php
  - routes/channels.php
  - wave/src/Http/Middleware/TokenMiddleware.php
  - wave/src/Jobs/CreateActivityLog.php
  - app/Jobs/Middleware/EnsureAccountActive.php
  - tests/Feature/AccountSuspensionMiddlewareTest.php
autonomous: true
---

<objective>
Enforce account status centrally across all entry points and harden protected paths against bypass.

Purpose: If an account is restricted or suspended, that state must be enforced before application business logic.
Output: middleware + routes + job checks that consistently prevent restricted/suspended users from proceeding.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-account-suspension/02-CONTEXT.md
@.planning/codebase/ARCHITECTURE.md

@app/Http/Middleware/Authenticate.php
@app/Models/User.php
@app/Models/Organization.php
@wave/src/Jobs/CreateActivityLog.php
@wave/src/Http/Middleware/TokenMiddleware.php
@app/Jobs/Middleware/EnsureAccountActive.php
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement shared account status middleware and restricted route</name>
  <files>app/Http/Middleware/AccountStatusMiddleware.php, app/Http/Controllers/AccountRestrictedController.php, resources/views/account/restricted.blade.php</files>
  <action>
  Add `app/Http/Middleware/AccountStatusMiddleware.php` that runs before auth-sensitive access and:

  - Uses user + organization effective status to decide access
  - Redirects restricted users to `/account/restricted`
  - Hard-blocks suspended users with a 403-like response and support reference
  - Allows allowlisted routes:
    - `/account/restricted`
    - `/billing/*`
    - `/support/*`
    - `/data-export`
    - `/livewire/*`

  Add a lightweight page/controller at `/account/restricted` showing reason/countdown and action links.
  Keep messaging neutral and non-credential exposing.
  </action>
  <verify>
  - Middleware file exists and runs without syntax errors
  - Restricted and suspended paths are distinguishable in behavior
  </verify>
  <done>Restricted/suspended checks run through one middleware and restricted users are routed through a dedicated page</done>
</task>

<task type="auto">
  <name>Task 2: Wire middleware into web/API/broadcast and queue/job checks</name>
  <files>bootstrap/app.php, routes/web.php, routes/api.php, routes/channels.php, app/Jobs/Middleware/EnsureAccountActive.php, wave/src/Http/Middleware/TokenMiddleware.php, wave/src/Jobs/CreateActivityLog.php</files>
  <action>
  Register `AccountStatusMiddleware` in the web stack and ensure API auth paths include it.

  Update route guard points:
  - `routes/web.php` enforce middleware group
  - `routes/api.php` apply status checks to token-authenticated routes
  - `routes/channels.php` enforce account checks for channel authorization

  Create/update queue safety:
  - `app/Jobs/Middleware/EnsureAccountActive` checks account status before job execution
  - Wire into queue pipeline where queue jobs are dispatched
  - Ensure `wave/src/Jobs/CreateActivityLog.php` re-checks status at execution
  - Update `wave/src/Http/Middleware/TokenMiddleware.php` to reject token usage for restricted/suspended users
  </action>
  <verify>Restricted/suspended statuses are checked in every configured entry point path</verify>
  <done>Middleware is configured for web, api, broadcast, and queue/job execution</done>
</task>

<task type="auto">
  <name>Task 3: Test the gate behavior and edge cases</name>
  <files>tests/Feature/AccountSuspensionMiddlewareTest.php</files>
  <action>
  Add feature tests for:
  - Restricted users are redirected to `/account/restricted`
  - Suspended users receive block response and cannot access protected routes
  - Sanctioned Sanctum token requests are rejected
  - Job middleware blocks execution when status changed after dispatch
  - Broadcast/channel authorization respects current account state
  </action>
  <verify>
  `php artisan test --compact tests/Feature/AccountSuspensionMiddlewareTest.php`
  </verify>
  <done>Middleware behavior is covered for web, api, tokens, and queue paths</done>
</task>

</tasks>

<verification>
- [ ] `AccountStatusMiddleware` exists and is in the web and API middleware stacks
- [ ] `/account/restricted` route works and is allowlisted
- [ ] Suspended access is denied without a bypass on web/api/channel/job/token paths
- [ ] Tests for middleware and job status checks pass
</verification>

<success_criteria>
- Status enforcement is uniform and early across all entry points
- No request can bypass status checks in documented entry points
- Queue and token flows re-evaluate status at execution time
</success_criteria>

<output>
After completion, create `.planning/phases/02-account-suspension/02-02-SUMMARY.md`
</output>

<verification>
- [ ] `AccountStatusMiddleware` is wired to web middleware stack and API auth paths.
- [ ] Restricted user sessions are redirected to `/account/restricted`.
- [ ] Suspended users are hard-blocked with no route model bypass.
- [ ] Sanctum / API-token and queue execution paths re-check status at runtime.
- [ ] Feature test suite for middleware coverage passes.
</verification>

<success_criteria>
- Middleware is early and consistent across web, API, and queue execution paths.
- No request can bypass account status checks in documented entry points.
- Job execution of existing queued jobs validates account status before sensitive work.
</success_criteria>

<output>
After completion, create `.planning/phases/02-account-suspension/02-02-SUMMARY.md`
</output>
