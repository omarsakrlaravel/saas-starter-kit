---
phase: 02-account-suspension
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/Enums/AccountStatus.php
  - app/Models/User.php
  - app/Models/Organization.php
  - database/migrations/2026_03_05_000001_add_account_status_to_users_and_organizations.php
  - database/migrations/2026_03_05_000002_create_account_status_histories_table.php
  - tests/Feature/AccountStatusModelTest.php
autonomous: true
---

<objective>
Define the account state machine for both users and organizations and persist reasoned suspension history.

Purpose: Every account lifecycle transition must be explicit and queryable so middleware and admin actions can make deterministic decisions.
Output: Status enum, status columns/migrations, and model-level helpers for active/restricted/suspended checks.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-account-suspension/02-CONTEXT.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md

@app/Models/User.php
@app/Models/Organization.php
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create shared account status enum</name>
  <files>app/Enums/AccountStatus.php</files>
  <action>
    Add `app/Enums/AccountStatus.php` with three values: `Active`, `Restricted`, and `Suspended`.

    Include helper methods:
    - `public function isBlocking(): bool` to indicate access block.
    - `public function isRestricted(): bool` and `isSuspended(): bool`.
    - `public function label(): string` for admin-facing display.
  </action>
  <verify>Enum file exists and compiles under strict PHP 8.4 rules.</verify>
  <done>A valid enum is added and can be imported by both User and Organization models.</done>
</task>

<task type="auto">
  <name>Task 2: Add account status columns and history table</name>
  <files>
    database/migrations/2026_03_05_000001_add_account_status_to_users_and_organizations.php
    database/migrations/2026_03_05_000002_create_account_status_histories_table.php
  </files>
  <action>
  Create two migrations that preserve existing columns and constraints:

  1. `2026_03_05_000001_add_account_status_to_users_and_organizations.php`
     - Add `status` to `users` and `organizations`:
       - type string, default `active`
       - index for filtering and reporting
     - Add `status_reason` (nullable string) and `status_expires_at` (nullable datetime) to both tables.
     - Keep existing `active` columns untouched to avoid regressions.
     - Add downgrade logic for all added columns and constraints.

  2. `2026_03_05_000002_create_account_status_histories_table.php`
     - `suspendable_type`, `suspendable_id`, `from_status`, `to_status`, `reason`, `reference_code`, `details`
     - `applied_by` nullable FK to users
     - `expires_at`, `applied_at` timestamps, plus indexes on type/id and status transitions.
     - Foreign key strategy should keep history resilient if users/orgs are deleted.
  </action>
  <verify>Migration files are present with complete `up` and `down` methods and pass `php -d detect_unicode=0 -l` style syntax checks.</verify>
  <done>Both status and status history tables can migrate up and down safely.</done>
</task>

<task type="auto">
  <name>Task 3: Add model-level status behavior and tests</name>
  <files>app/Models/User.php, app/Models/Organization.php, tests/Feature/AccountStatusModelTest.php</files>
  <action>
  Update both models to use `AccountStatus`:

  - Add `status`, `status_reason`, and `status_expires_at` to `$fillable`.
  - Add casts for `status` and expiry datetime.
  - Add helpers:
    - `isActiveAccount()`, `isRestricted()`, `isSuspended()`, and `isBlockedFromSession()`
    - `statusDisplay()`
  - Add scopes:
    - `scopeWithStatus(string $status)`
    - `scopeActiveOrRestricted()` where needed for admin reporting.
  - Organization and User should expose a method to check the peer entity's effective status:
    - `organizationIsBlocked()`, `activeOrganizationOrSelf()` logic for later middleware reuse.

  Add `tests/Feature/AccountStatusModelTest.php` with cases for cast behavior and helper outputs on both user and organization.
  </action>
  <verify>
  `php artisan test --compact tests/Feature/AccountStatusModelTest.php`
  </verify>
  <done>Models expose clear status state checks and tests verify casted enum behavior and helper methods.</done>
</task>

</tasks>

<verification>
- [ ] Status enum and status migration files exist and are consistent.
- [ ] `users.status` and `organizations.status` store enum-like values and have indexes.
- [ ] `account_status_histories` table exists with typed relationship and nullable fallback fields.
- [ ] `AccountStatusModelTest` passes: status helper methods and casts all behave as expected.
</verification>

<success_criteria>
- A single source of truth for status exists for both User and Organization.
- Status transitions are auditable through `account_status_histories`.
- All status model helpers are covered by tests.
</success_criteria>

<output>
After completion, create `.planning/phases/02-account-suspension/02-01-SUMMARY.md`
</output>
